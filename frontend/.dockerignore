# =============================================================================
# frontend 构建 context 排除（生产镜像在容器内构建 dist，dev 镜像用 bind-mount）
# =============================================================================

node_modules
dist
logs
coverage
*.log

# 敏感文件
.env
.env.*
!.env.example
