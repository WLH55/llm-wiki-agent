# SDD Spec: Dramatiq 任务队列迁移

> **RIPER Phase**: Review
> **Status**: Executed -- Review 进行中
> **Approval**: 已执行（代码已落盘 commit 55b1d78，review_execute 评审中）
> **Spec Path**: `mydocs/specs/2026-08-05_00-00_Dramatiq任务队列迁移.md`
> **Created**: 2026-08-05
> **Last Updated**: 2026-08-05（二轮修复：T2 测试重写 + T-asyncio + T6/T7/T9）

---

## 0. Open Questions

- [ ] Q1: handler 幂等策略调整--当前 handler 抛 `TerminalTaskError` 拒绝重做，用户重试会被挡。后续重写 handler 时需调整为允许重做已完成的步骤。
  - **review_execute 补注（2026-08-05）**：当前 `_handle_failure` 对 Transient 错误也调 `fail_run`，导致 Dramatiq `max_retries=3` 自动重试失效（重试消息被 `claim_run` 吸收，Run 已 failed）。此为**已知限制**（见 `mydocs/task-queue-design.md` §4.3/§7），归属 Q1 整体处理：Q1 需同时做 (1) 重试时 reset Run 为 pending；(2) handler 幂等从"拒绝重做"改为"允许重做已完成步骤"。本次 review 不单独修，保持 `max_retries=3` 配置不变。
  - **attempt_no 双层级分析补注（2026-08-05）**：
    - **问题**：`processing_runs.attempt_no`（业务级，手动重跑链序号，自动重试不递增）与 `processing_spans.attempt_no`（执行级，每次消息执行应递增）被混为一谈。当前 `RunIdentity.attempt_no` 从 `run.attempt_no` 读（业务级），`span_tracker` 直接用它写 span 的 `attempt_no`，而 `start_worker_attempt` 创建 `worker_attempt` span 时甚至没设置 `attempt_no`（默认 1），执行序号只藏在 `metrics["attempt"]` 里。
    - **后果**：自动重试时同一 `(run_id, attempt_no=1, span_name)` 唯一键冲突，span 被 swallow 不更新。当前重试不生效不触发，Q1 修复重试后会暴露。
    - **两个层级的设计意图**：
      - `processing_runs.attempt_no`：业务重跑链序号，用户手动重试创建新 Run 时递增，`retry_of_run_id` 指向旧 Run。回答"这是用户第几次重跑这个任务？"自动重试不递增（同一 Run）。
      - `processing_spans.attempt_no`：执行层序号，每次 worker 消息执行（含自动重试）递增。与 `(run_id, span_name)` 构成唯一键，让同一阶段的每次尝试是独立 span 行。`worker_attempt` span 是执行主轴，业务 span（PARSE/PERSIST/EMBED/ACTIVATE）是其子阶段。
    - **修复思路（归属 Q1）**：
      1. `RunIdentity` 新增 `execution_attempt` 字段（执行序号），与 `run.attempt_no`（业务序号）分离
      2. `start_worker_attempt` 把 COUNT+1 写入 `span.attempt_no`（不只放 `metrics`），并返回给调用方
      3. `execute_run_message` 用 `start_worker_attempt` 返回的 `span.attempt_no` 填充 `RunIdentity.execution_attempt`
      4. `span_tracker` 用 `ctx.identity.execution_attempt` 而非 `run.attempt_no` 写业务 span 的 `attempt_no`
      5. 同时实现 `reset_run_for_retry`（CAS running->pending + `execution_attempt` 递增），让 Dramatiq 重试时 Run 回 pending 且执行序号 +1
- [x] Q2: ~~Span 心跳 API 设计~~--已决策：by-name shim + ctx pattern（`begin_span(ctx, name)` / `end_span(ctx, name)`），对齐 WeKnora SpanTracker 4 事件（Begin/End/Fail/Skip）。
- [x] Q3: ~~Span 心跳阈值与长阶段协调~~--已决策：70min 阈值 + 粗粒度事件驱动，删 `touch_span`，长阶段内部不刷新。
- [x] Q4: ~~失败回调 Middleware 的 async 适配~~--已决策：用 `asyncio.run()`（不复用 EventLoopThread，避免依赖 Dramatiq 私有 API）。
- [x] Q5: ~~docker-compose 服务调整~~--已决策：删 outbox-publisher，合并为单个 task-worker。
- [x] Q6: ~~Run 状态机简化~~--已决策：删 token/epoch/lease/heartbeat 字段，保留简化版 CAS。
- [x] Q7: ~~Dramatiq time_limit 配置~~--已决策：所有 actor 统一 `time_limit=3600000`（1h）。
- [x] Q8: ~~commit_success 失败的已知限制~~--已接受不完美，不为罕见场景过度设计。

---

## 1. Requirements (Context)

- **Goal**: 将任务队列从 Taskiq + Redis Stream + Transactional Outbox 自建投递层，迁移到 Dramatiq + Redis，砍掉自建投递复杂度，借鉴 WeKnora 的成熟框架兜底哲学。

- **In-Scope**:
  - 删除 `task_outbox` 表和 `outbox/` 整个目录（Outbox Publisher + Reaper）
  - 删除 fencing CAS（token/epoch/lease/heartbeat 字段和校验逻辑）
  - 删除心跳续租（`_heartbeat_loop`）、退避计算（`_retry_delay`）、`retry_run`
  - 删除 multimodal/low 队列，队列拓扑简化为 critical + default
  - Taskiq `RedisStreamBroker` 换成 Dramatiq `RedisBroker`
  - 同函数多 actor 注册（方案 C）实现 critical/default 队列路由
  - 自定义 `RunFailureMiddleware`（`after_process_message` 钩子标 Run failed）
  - 嵌入 worker 的简化版 Reaper（扫 span 心跳 + pending 过旧）
  - Span 心跳 API（`begin_span` / `end_span`），handler 各阶段调用
  - docker-compose 调整：删 outbox-publisher，合并 worker 容器
  - `processing_runs` 表字段清理（删 fencing 相关字段）
  - Alembic 迁移脚本

- **Out-of-Scope**:
  - handler 逻辑重写（仅调整 span 心跳调用，幂等策略调整留后续任务）
  - handler 幂等策略从"拒绝重做"改为"允许重做已完成步骤"（后续任务）
  - 前端 UI 适配
  - 性能压测

---

## 1.1 Context Sources

- **Requirement Source**: 用户在 grilling 会话中逐题确认的 21 个决策点
- **Design Refs**:
  - `mydocs/dramatiq-migration-decision.md`（grilling 决策记录，21 个问题逐项确认）
  - `mydocs/task-queue-comparison-weknora-vs-llm-wiki-agent.md`（WeKnora 对比分析）
  - `D:/AI/WeKnora/internal/application/service/knowledge_housekeeping.go`（housekeeping 源码）
  - `D:/AI/WeKnora/internal/application/service/knowledge_span_tracker.go`（SpanTracker 源码）
- **Chat/Business Refs**: grilling 会话全程（问题 1-21）
- **Extra Context**:
  - Dramatiq 源码：`dramatiq/asyncio.py`、`dramatiq/middleware/retries.py`、`dramatiq/brokers/redis.py`、`dramatiq/middleware/middleware.py`
  - Dramatiq 官方文档：User Guide、Advanced Topics、Cookbook、API Reference

---

## 1.5 Codemap Used (Feature/Project Index)

- **Codemap Mode**: feature（基于现有代码结构，非全量扫描）
- **Codemap File**: 无独立 codemap 文件，关键索引如下：
- **Key Index**:
  - **入口点**: `backend/app/workers/core/broker.py`（Taskiq broker 配置）、`backend/app/workers/core/tasks.py`（actor 注册）
  - **核心逻辑**:
    - `backend/app/workers/core/executor.py`：`execute_run_message`（claim + handler 派发 + commit）
    - `backend/app/workers/core/runtime.py`：`claim_run` / `complete_run` / `fail_run` / `retry_run` / `renew_lease`
    - `backend/app/workers/outbox/`：`outbox.py` / `publisher.py` / `outbox_service.py` / `outbox_worker.py` / `reaper.py`
  - **数据模型**: `backend/app/models/task_runtime.py`（ProcessingRun / ProcessingSpan / TaskOutbox）
  - **配置**: `backend/app/config/settings.py`（TASK_* 参数）、`docker-compose.yml`（worker 容器拓扑）
  - **外部依赖**: Taskiq + taskiq_redis（待删）、Dramatiq + dramatiq[redis]（待加）

---

## 1.6 Context Bundle Snapshot (Lite)

- **Bundle Level**: Lite
- **Bundle File**: 无独立 bundle 文件，关键事实如下：
- **Key Facts**:
  - 当前任务队列：Taskiq + Redis Stream + Transactional Outbox（三表运行时）
  - 全部任务是 IO 密集（embedding 调 Jina API，非本地模型）
  - WeKnora 用 asynq（Go）+ housekeeping + span 心跳，无 fencing CAS
  - Dramatiq 的 `queue_name` 在 actor 定义时固定，不能 per-message 覆盖（源码验证）
  - Dramatiq Retries middleware 重试时用原 message enqueue，queue_name 是 frozen 字段，重试回原队列
  - Dramatiq AsyncIO middleware 的并发能力和 Taskiq 等价（源码验证）
  - Dramatiq Redis broker 的崩溃重投靠 heartbeat_timeout（默认 60s），不是 visibility timeout
- **Open Questions**: 见 §0

---

## 2. Research Findings

### 2.1 当前架构事实

**当前任务运行时三表分工**：
- `processing_runs`：任务运行账本（终态真相源）
- `processing_spans`：阶段观测层（诊断流水）
- `task_outbox`：可靠投递层（消息意图）

**当前 worker 拓扑**（docker-compose.yml）：
- `task-worker-shared`：`taskiq worker --workers 1 --max-async-tasks 14`（消费 default + critical + multimodal + low）
- `task-worker-critical`：`taskiq worker --workers 1 --max-async-tasks 2`（只消费 critical）
- `outbox-publisher`：独立进程，轮询 Outbox 投递到 Redis Stream

**当前 handler 行为**（`rag_ingestion.py`）：
- handler 是"从头重做 + 幂等检查"模式，不是"接管续跑"
- `document_process_handler`：幂等检查是"已有 chunks 则抛 `candidate_chunks_exist`"
- `rag_index_handler`：幂等检查是"已有 embedding 则抛 `candidate_chunks_indexed`"
- 两个 handler 的幂等检查都是**拒绝重做**，用户重试会被挡

### 2.2 WeKnora 架构事实

**WeKnora 的 housekeeping**（`knowledge_housekeeping.go`）：
- 嵌入主进程的 cron，每 5 分钟扫一次
- 扫描 `parse_status IN ('processing','finalizing') AND updated_at < cutoff` 的 knowledge 行
- 阈值 = max(1h, DocumentProcessTimeout) + 10min = 70min
- 两道防误杀关卡：span 心跳（`MAX(spans.updated_at)`）+ 队列探针（`HasQueuedTasksForKnowledge`）
- 动作：标 `parse_status='failed'`，不自动重投

**WeKnora 的 SpanTracker**（`knowledge_span_tracker.go`）：
- 事件驱动心跳：每个阶段的 `BeginStage` / `EndSpan` / `FailSpan` / `SkipSpan` 都调 `touchKnowledgeHeartbeat`
- `touchKnowledgeHeartbeat` 刷 `knowledge.updated_at`（root/stage 级），subspan/generation 不刷父行避免写放大
- housekeeping 扫描时读 `MAX(spans.updated_at)` 作为"最近活动时间"

**WeKnora 的普通任务没有 lease/fencing/续租**，靠 asynq 的 Timeout（2h）+ 死信回调 + housekeeping。

### 2.3 Dramatiq 框架事实

**Dramatiq Redis broker 崩溃重投**（`redis.py` 源码）：
- `DEFAULT_HEARTBEAT_TIMEOUT = 60000`（60s）
- worker 进程定期往 Redis 写心跳（用 `broker_id` 标识）
- worker 进程崩溃 -> 心跳停止 -> 60s 后未 ack 消息被 maintenance 过程放回队列
- 这是**进程级**机制，不是消息级

**Dramatiq Retries middleware**（`retries.py` 源码）：
- 重试时 `broker.enqueue(message, delay=delay)`，用原 message
- `message.queue_name` 是 frozen dataclass 字段，重试回原队列
- `throws` 选项：指定异常类型不重试，直接 `message.fail()` 进死信
- `max_retries` / `min_backoff` / `max_backoff` / `retry_when` 可配

**Dramatiq AsyncIO middleware**（`asyncio.py` 源码）：
- 每个 worker 进程 1 个 `EventLoopThread`（全局单例）
- worker 线程调 async actor 时，`run_coroutine_threadsafe` 提交协程到事件循环
- worker 线程阻塞等结果，事件循环继续跑其他协程
- 并发协程数 = `--threads` 数，和 Taskiq 的 `--max-async-tasks` 等价

**Dramatiq queue_name 固定**（`actor.py` 源码）：
- `message_with_options` 中 `queue_name=self.queue_name` 硬编码
- `send_with_options(queue_name="xxx")` 不会改变消息队列，会掉进 `options` 字典被忽略
- 方案 C（同函数多 actor 注册）是实现多队列路由的正确方式

**Dramatiq Middleware 生命周期钩子**（`middleware.py` 源码）：
- `after_process_boot`：worker 子进程启动后（子进程内）--用于启动 Reaper
- `after_process_message(broker, message, *, result=None, exception=None)`：消息处理后--用于失败回调
- `before_worker_shutdown`：worker 关闭前--用于停止 Reaper

### 2.4 风险与不确定项

1. **commit_success 失败的已知限制**：DB 临时故障导致 `commit_success` 失败时，业务可能已部分写入，但 Run 最终被 Reaper 标 failed。当前接受这个不完美。

2. **Span 心跳阈值与长阶段协调**：Reaper 5min 扫一次，span 心跳阈值 5min。如果 handler 某个阶段执行超过 5min（如 embedding 5K chunks），span 不刷新，Reaper 会误杀。需要 handler 在长阶段内部定期刷新 span。

3. **双写窗口**：删 Outbox 后，DB 提交后、入队前崩溃的窗口可能丢任务。靠 Reaper 扫"pending 且 created_at 过旧"兜底。

4. **Dramatiq Redis broker 心跳超时（60s）与 span 心跳的交互**：worker 进程崩溃后 60s，Dramatiq 重投消息。新 worker 调 `claim_run`，Run 状态是 running（旧 worker 领取过）-> CAS `status='pending'` 失败 -> 消息 ack 丢弃。Run 卡 running -> 5min 后 span 心跳超时 -> Reaper 标 failed。恢复延迟约 5-10 分钟。

5. **handler 幂等策略**：当前 handler 幂等检查是拒绝重做，用户重试会被挡。后续重写 handler 时调整。

---

## 2.1 Next Actions

1. 用户确认 Research 结论后，进入 Plan 阶段
2. Plan 阶段产出：文件变更清单、函数签名、原子化 checklist
3. Plan 完成后，用户执行 `review_spec`（建议性预审）
4. 用户确认 `Plan Approved` 后，进入 Execute 阶段

---

## 3. Innovate (Skipped)

- **Skipped**: true
- **Reason**: grilling 会话已逐题确认所有关键决策（21 个问题），方案已收敛，无需多方案比较。核心决策链见 §2.5。

### 2.5 决策结论总览（grilling 21 题汇总）

| # | 决策项 | 结论 | 理由 |
|---|---|---|---|
| 1 | 通用 Outbox | **删除** | WeKnora 验证"直接入队 + 恢复扫描"够用；任务均可重试可重建 |
| 2 | 投递框架 | **换 Dramatiq + Redis** | Taskiq 投递能力不全，Dramatiq 内置重试/死信/延迟/崩溃重投 |
| 3 | FastAPI 适配 | 适配，无摩擦 | 进程分离天然兼容；入队侧 `actor.send()` 更简单 |
| 4 | 并发模型 | 单进程多线程（`--threads 16`） | 全部 IO 密集，多线程与多协程并发能力等价（源码验证） |
| 5 | Python 并发分析 | 全 IO 密集 | embedding 调 Jina API，非本地模型 |
| 6 | 六项可靠性保证 | 重试/死信/延迟/崩溃重投交 Dramatiq；持久化保留账本；lease 改 span 心跳 | 逐项源码验证。**review_execute 修正**：自动重试当前实际不生效（见 Q1 补注），重试/死信机制保留但重跑能力待 Q1 补齐 |
| 7 | 多队列路由 | 方案 C：同函数多 actor 注册 | Dramatiq queue_name 固定死（源码验证） |
| 8 | 弹性借用 | 放弃 | Dramatiq 多队列订阅是平级消费 |
| 9 | multimodal 队列 | 删除 | 当前未使用 |
| 10 | low 队列 | 删除 | wiki_generate 改到 default |
| 11 | 消息内容 | 只传 run_id | worker 回 DB 读 Run 快照 |
| 12 | 重试队列路由 | Dramatiq 自动回原队列 | Retries middleware 用原 message enqueue（源码验证） |
| 13 | Worker 容器拓扑 | 单容器 `--queues critical default --threads 16` | 先启动一个进程 |
| 14 | Fencing | 删除（token/epoch/lease 全删）| handler 已有幂等检查，CAS 冗余 |
| 15 | Reaper 触发 | 嵌入 worker，5min 扫一次 | `after_process_boot` 启独立线程 |
| 16 | Reaper 动作 | 标 Run failed，不重投 | 对齐 WeKnora；用户手动重试 |
| 17 | 失败回调 | 自定义 Middleware，复用 EventLoopThread | `after_process_message` 钩子 |
| 18 | Actor 模式 | 路径 C：actor 薄包装，保留 executor | `execute_run_message` 保留 claim/派发/commit |
| 19 | claim 失败 | actor 正常返回，Dramatiq ack | 重复投递被吸收 |
| 20 | 判卡死依据 | span 心跳（删 lease）| 事件驱动刷新，对齐 WeKnora |
| 21 | time_limit | 统一 3600000ms（1h）| span 心跳 + Reaper 是主要机制 |

---

## 4. Plan (Contract)

> **RIPER Phase**: Plan
> **Status**: [LOCKED] -- 等待 `Plan Approved`

### 4.1 File Changes

#### 新增文件

| 文件路径 | 变更说明 |
|---|---|
| `backend/app/workers/core/constants.py` | **review_execute 补登**：集中管理 RunStatus/SpanStatus/RunType/ScopeType/TriggerType/ErrorCode/SpanName 枚举 + 队列常量 + CRITICAL_RUN_TYPES，避免字符串字面量分散 |
| `backend/app/workers/core/middleware.py` | 新增 `RunFailureMiddleware`（`after_process_message` 标 Run failed）和 `ReaperMiddleware`（`after_process_boot` 启 Reaper 线程，`before_worker_shutdown` 停止）|
| `backend/app/workers/core/span_tracker.py` | 新增 Span 心跳 API（by-name shim + ctx pattern）：`begin_span` / `end_span` / `fail_span` / `skip_span`，对齐 WeKnora SpanTracker 4 事件；删 `touch_span` |
| `backend/app/workers/core/reaper.py` | 新增简化版 Reaper：`recover_stalled_runs`（扫 span 心跳超时 + pending 过旧）+ `run_reaper_loop`（独立线程循环）|
| `backend/alembic/versions/002_dramatiq_migration.py` | Alembic 迁移：删 `task_outbox` 表，删 `processing_runs` 的 fencing 字段；**review_execute 补**：upgrade 前清理 processing_spans 历史重复行 |

#### 修改文件

| 文件路径 | 变更说明 |
|---|---|
| `backend/pyproject.toml` | 删 `taskiq` / `taskiq-redis`，加 `dramatiq[redis,watch]` |
| `backend/app/config/settings.py` | 删 `TASK_LEASE_SECONDS` / `TASK_HEARTBEAT_SECONDS` / `TASK_STREAM_IDLE_TIMEOUT_MS` / `TASK_OUTBOX_*`；加 `TASK_REAPER_INTERVAL_SECONDS=300` / `TASK_SPAN_STALE_SECONDS=4200` / `TASK_PENDING_STALE_SECONDS=300` / `TASK_TIME_LIMIT_MS=3600000` |
| `backend/app/workers/core/broker.py` | 重写：Taskiq `RedisStreamBroker` -> Dramatiq `RedisBroker`；删 `MULTIMODAL_QUEUE` / `LOW_QUEUE` / `_broker` / `create_shared_broker` / `create_critical_broker`；加 AsyncIO middleware + `RunFailureMiddleware` + `ReaperMiddleware` |
| `backend/app/workers/core/tasks.py` | 重写：Taskiq `@broker.task` -> Dramatiq `@dramatiq.actor`；同函数多 actor 注册（`process_run_default` + `process_run_critical`）；删 `send_task_message` / `QUEUE_ALIASES`；加 `enqueue_run(run_type, run_id)` 按队列选 actor |
| `backend/app/workers/core/runtime.py` | 简化 `claim_run`（删 token/epoch，只 CAS status）；简化 `complete_run` / `fail_run`（删 lease 校验）；删 `renew_lease` / `retry_run` / `create_run_with_outbox`；加 `create_run`（只写 Run，不写 Outbox）+ `mark_run_enqueue_failed` |
| `backend/app/workers/core/executor.py` | 删 `_heartbeat_loop` / `_retry_delay` / `_default_jitter`；简化 `execute_run_message`（删心跳启动/停止、删退避计算）；`_handle_failure` 简化为只调 `fail_run`（不调 `retry_run`）；删 `queue_name_for_run` 中的 `LOW_QUEUE` 分支；`RunExecutionContext` 删 `renew` / `_lease_lock` / `lease` / `lease_seconds`；`RunIdentity` 删 lease 相关 + 加 `attempt_no` 字段（用于 span 唯一键） |
| `backend/app/workers/core/errors.py` | 删 `LeaseLostError` |
| `backend/app/workers/__init__.py` | 更新公开 API：删 `LOW_QUEUE` / `MULTIMODAL_QUEUE` / `create_run_with_outbox` / `send_task_message` / `LeaseLostError`；加 `create_run` / `enqueue_run` / `begin_span` / `end_span` |
| `backend/app/models/task_runtime.py` | `ProcessingRun`：删 `execution_token` / `execution_epoch` / `lease_expires_at` / `worker_id` / `heartbeat_at`；删 `TaskOutbox` 类；`ProcessingSpan`：加 `attempt_no` 字段（span 唯一键） |
| `backend/app/knowledge_bases/service/rag_ingestion.py` | `create_run_with_outbox` -> `create_run` + `enqueue_run`；加 span 心跳调用（`begin_span` / `end_span`）|
| `backend/app/parsers/service/document.py` | `create_run_with_outbox` -> `create_run` + `enqueue_run` |
| `docker-compose.yml` | 删 `outbox-publisher` 服务；合并 `task-worker-shared` + `task-worker-critical` 为单个 `task-worker`（`dramatiq app.workers.core.tasks --queues critical default --threads 16`）|

#### 删除文件

| 文件路径 | 变更说明 |
|---|---|
| `backend/app/workers/outbox/outbox.py` | 删除整个文件 |
| `backend/app/workers/outbox/outbox_service.py` | 删除整个文件 |
| `backend/app/workers/outbox/outbox_worker.py` | 删除整个文件 |
| `backend/app/workers/outbox/publisher.py` | 删除整个文件 |
| `backend/app/workers/outbox/reaper.py` | 删除整个文件（功能迁移到 `workers/core/reaper.py`）|
| `backend/app/workers/outbox/__init__.py` | 删除整个目录 |

### 4.2 Signatures

#### `backend/app/workers/core/broker.py`

```python
import dramatiq
from dramatiq.brokers.redis import RedisBroker

# 队列常量（删 multimodal/low，只保留 critical + default）
CRITICAL_QUEUE: str = "critical"
DEFAULT_QUEUE: str = "default"

# 全局 broker 实例
broker: RedisBroker

def create_broker(redis_url: str) -> RedisBroker:
    """构造 Dramatiq RedisBroker，注册 AsyncIO + RunFailure + Reaper 中间件。"""
```

#### `backend/app/workers/core/tasks.py`

```python
import dramatiq

# 同函数多 actor 注册（方案 C）
@dramatiq.actor(queue_name="default", max_retries=3, min_backoff=5000,
                max_backoff=300000, time_limit=3600000,
                throws=(TerminalTaskError,))
async def process_run_default(run_id: int) -> str:
    """default 队列入口：处理 document_process / wiki_generate 等。"""

@dramatiq.actor(queue_name="critical", max_retries=3, min_backoff=5000,
                max_backoff=300000, time_limit=3600000,
                throws=(TerminalTaskError,))
async def process_run_critical(run_id: int) -> str:
    """critical 队列入口：处理 rag_index / source_sync 等。"""

def enqueue_run(run_type: str, run_id: int) -> None:
    """按 run_type 选 actor 入队。critical -> process_run_critical；其余 -> process_run_default。"""
```

#### `backend/app/workers/core/runtime.py`

```python
async def create_run(
    db: AsyncSession,
    *,
    tenant_id: int, kb_id: int, run_type: str, scope_type: str, scope_id: int,
    trigger_type: str, parent_run_id: int | None = None,
    retry_of_run_id: int | None = None, attempt_no: int = 1,
    idempotency_key: str | None = None, effective_config_version: int | None = None,
    options_snapshot: dict | None = None, requested_by_user_id: int | None = None,
) -> ProcessingRun:
    """创建 Run 行（不写 Outbox），调用方事务内调用。"""

async def claim_run(
    db: AsyncSession, run_id: int, *, worker_id: str
) -> bool:
    """原子 CAS：pending -> running。返回是否领取成功。"""

async def complete_run(
    db: AsyncSession, run_id: int
) -> bool:
    """原子 CAS：running -> succeeded。返回是否成功。"""

async def fail_run(
    db: AsyncSession, run_id: int, *, error_code: str, error_message: str
) -> bool:
    """原子 CAS：running -> failed。返回是否成功。"""

async def mark_run_enqueue_failed(
    db: AsyncSession, run_id: int, *, error_message: str
) -> None:
    """入队失败时标 Run failed，用户可见可重试。"""
```

#### `backend/app/workers/core/executor.py`

```python
@dataclass(frozen=True)
class RunIdentity:
    """同当前，但不含 lease 相关；加 attempt_no 用于 span 唯一键。"""
    run_id: int
    tenant_id: int
    kb_id: int
    run_type: str
    scope_type: str
    scope_id: int
    attempt_no: int
    options_snapshot: dict[str, Any]

class RunExecutionContext:
    """删 lease / renew / _lease_lock / lease_seconds。"""
    identity: RunIdentity
    session_factory: async_sessionmaker[AsyncSession]
    committed: bool

    async def commit_success(self, writer: SuccessWriter | None = None) -> None:
        """原子提交业务结果 + complete_run（CAS running->succeeded）。"""

async def execute_run_message(
    run_id: int,
    *,
    worker_id: str,
    handlers: Mapping[str, RunHandler],
    session_factory: async_sessionmaker[AsyncSession],
) -> ExecutionOutcome:
    """消费消息：claim_run -> start_worker_attempt -> 执行 handler -> commit_success。
    删心跳、删退避、删 retry_run 调用。handler 抛异常时冒泡给 Dramatiq Retries middleware。"""

def queue_name_for_run(run_type: str) -> str:
    """简化：critical 队列类型 -> CRITICAL_QUEUE；其余 -> DEFAULT_QUEUE。"""
```

#### `backend/app/workers/core/span_tracker.py`（新增）

```python
async def begin_span(
    ctx: RunExecutionContext,
    span_name: str,
    *,
    input_summary: dict | None = None,
) -> None:
    """开始阶段 span：(run_id, attempt_no, span_name) 唯一。
    best-effort：DB 错误 log + swallow，不阻断流水线。刷 spans.updated_at 作为心跳。"""

async def end_span(
    ctx: RunExecutionContext,
    span_name: str,
    *,
    output_summary: dict | None = None,
) -> None:
    """结束 span：status=succeeded + finished_at + output_summary。best-effort。"""

async def fail_span(
    ctx: RunExecutionContext,
    span_name: str,
    *,
    error_code: str,
    error_message: str,
) -> None:
    """失败 span：status=failed + finished_at + error_code/message。best-effort。"""

async def skip_span(
    ctx: RunExecutionContext,
    span_name: str,
    *,
    reason: str,
) -> None:
    """跳过 span（幂等检查命中跳过阶段）：status=skipped + finished_at。best-effort。"""
```

#### `backend/app/workers/core/reaper.py`（新增）

```python
async def recover_stalled_runs(
    session_factory: async_sessionmaker[AsyncSession],
    *,
    span_stale_seconds: float,
    pending_stale_seconds: float,
    batch_size: int = 100,
) -> list[int]:
    """扫描卡死 Run 并标 failed：
    1. running 且 MAX(spans.updated_at) 超 span_stale_seconds 没动
    2. pending 且 created_at 超 pending_stale_seconds 没动
    返回标 failed 的 run_id 列表。"""

def run_reaper_loop(
    session_factory: async_sessionmaker[AsyncSession],
    *,
    interval_seconds: float,
    span_stale_seconds: float,
    pending_stale_seconds: float,
    stop: threading.Event,
) -> None:
    """独立线程循环：每 interval_seconds 调一次 recover_stalled_runs。"""
```

#### `backend/app/workers/core/middleware.py`（新增）

```python
class RunFailureMiddleware(dramatiq.Middleware):
    """after_process_message 钩子：消息失败时标 Run failed。
    复用 Dramatiq EventLoopThread 调 async mark_run_failed。"""

class ReaperMiddleware(dramatiq.Middleware):
    """after_process_boot 启 Reaper 线程；before_worker_shutdown 停止。"""
```

### 4.3 Implementation Checklist

#### 阶段 1：依赖与配置（基础设施）

- [x] 1. `backend/pyproject.toml`：删 `taskiq` / `taskiq-redis`，加 `dramatiq[redis,watch]`
- [x] 2. `backend/app/config/settings.py`：删 `TASK_LEASE_SECONDS` / `TASK_HEARTBEAT_SECONDS` / `TASK_STREAM_IDLE_TIMEOUT_MS` / `TASK_OUTBOX_BATCH_SIZE` / `TASK_OUTBOX_LOCK_SECONDS` / `TASK_OUTBOX_POLL_SECONDS` / `TASK_CRITICAL_RESERVED_CONCURRENCY`；加 `TASK_REAPER_INTERVAL_SECONDS=300` / `TASK_SPAN_STALE_SECONDS=4200` / `TASK_PENDING_STALE_SECONDS=300` / `TASK_TIME_LIMIT_MS=3600000`
- [x] 3. `backend/app/workers/core/errors.py`：删 `LeaseLostError` 类

#### 阶段 2：数据模型与迁移

- [x] 4. `backend/app/models/task_runtime.py`：`ProcessingRun` 删 `execution_token` / `execution_epoch` / `lease_expires_at` / `worker_id` / `heartbeat_at` 字段；删 `TaskOutbox` 类；`ProcessingSpan` 加 `attempt_no` 字段（span 唯一键）
- [x] 5. `backend/alembic/versions/002_dramatiq_migration.py`：新建迁移脚本，`DROP TABLE task_outbox` + `ALTER TABLE processing_runs DROP COLUMN` 五个 fencing 字段 + `ALTER TABLE processing_spans ADD COLUMN attempt_no` + UNIQUE INDEX

#### 阶段 3：运行时核心（runtime + executor）

- [x] 6. `backend/app/workers/core/runtime.py`：删 `create_run_with_outbox` / `renew_lease` / `retry_run`；简化 `claim_run`（只 CAS status，不写 token/epoch/lease）；简化 `complete_run` / `fail_run`（只 CAS status，不校验 lease）；新增 `create_run`（只写 Run）+ `mark_run_enqueue_failed`
- [x] 7. `backend/app/workers/core/executor.py`：删 `_heartbeat_loop` / `_stop_heartbeat` / `_retry_delay` / `_default_jitter` / `_finish_cancelled_attempt`；简化 `execute_run_message`（删心跳启停、删退避计算、删 retry_run 调用）；`_handle_failure` 简化为只调 `fail_run`；`RunExecutionContext` 删 `lease` / `renew` / `_lease_lock` / `lease_seconds`；`RunIdentity` 删 lease 相关 + 加 `attempt_no` 字段（用于 span 唯一键）；`queue_name_for_run` 删 `LOW_QUEUE` 分支（wiki_generate -> default）
- [x] 8. `backend/app/workers/core/schemas.py`：`ExecutionOutcome` 删 `RETRY_SCHEDULED`（Dramatiq 接管重试，executor 不再调度重试）

#### 阶段 4：Span 心跳（新增）

- [x] 9. `backend/app/workers/core/span_tracker.py`：新增 `begin_span` / `end_span` / `fail_span` / `skip_span`（by-name shim + ctx pattern，对齐 WeKnora 4 事件）；删 `touch_span`；写入 `processing_spans` 表

#### 阶段 5：Reaper（新增）

- [x] 10. `backend/app/workers/core/reaper.py`：新增 `recover_stalled_runs`（扫 span 心跳超时 + pending 过旧，标 failed）+ `run_reaper_loop`（独立线程循环）

#### 阶段 6：Dramatiq 集成（broker + tasks + middleware）

- [x] 11. `backend/app/workers/core/broker.py`：重写为 Dramatiq `RedisBroker`；删 `MULTIMODAL_QUEUE` / `LOW_QUEUE` / `_broker` / `create_shared_broker` / `create_critical_broker`；加 AsyncIO + `RunFailureMiddleware` + `ReaperMiddleware`
- [x] 12. `backend/app/workers/core/middleware.py`：新增 `RunFailureMiddleware`（`after_process_message` 用 `asyncio.run()` 调 async `fail_run`）+ `ReaperMiddleware`（`after_process_boot` 启 Reaper 线程，`before_worker_shutdown` 停止）
- [x] 13. `backend/app/workers/core/tasks.py`：重写为 Dramatiq actor；同函数多 actor 注册（`process_run_default` + `process_run_critical`）；删 `send_task_message` / `QUEUE_ALIASES` / `worker_identity`；新增 `enqueue_run(run_type, run_id)` 按队列选 actor

#### 阶段 7：包初始化与业务调用方

- [x] 14. `backend/app/workers/__init__.py`：更新公开 API（删 `LOW_QUEUE` / `MULTIMODAL_QUEUE` / `create_run_with_outbox` / `send_task_message` / `LeaseLostError`；加 `create_run` / `enqueue_run` / `begin_span` / `end_span`）
- [x] 15. `backend/app/parsers/service/document.py`：`create_run_with_outbox` -> `create_run` + `enqueue_run`
- [x] 16. `backend/app/knowledge_bases/service/rag_ingestion.py`：`create_run_with_outbox` -> `create_run` + `enqueue_run`；handler 内加 span 心跳调用（`begin_span` / `end_span`）

#### 阶段 8：删除 Outbox

- [x] 17. 删除 `backend/app/workers/outbox/` 整个目录（`outbox.py` / `outbox_service.py` / `outbox_worker.py` / `publisher.py` / `reaper.py` / `__init__.py`）

#### 阶段 9：Docker 部署

- [x] 18. `docker-compose.yml`：删 `outbox-publisher` 服务；删 `task-worker-shared` + `task-worker-critical`；加单个 `task-worker` 服务（`dramatiq app.workers.core.tasks --queues critical default --threads 16`）

#### 阶段 10：验证

- [x] 19. 运行 `ruff check backend/` 确认无 lint 错误
- [ ] 20. 运行 `pytest backend/tests/` 确认现有测试通过（测试需适配新 API）-- **待后续任务**：3 个测试文件（test_task_runtime.py / test_task_executor.py / test_document_ingestion_runtime.py）共 28 个测试需大改或重写（引用 TaskOutbox / fencing / retry_run / renew_lease / RETRY_SCHEDULED 等已删除 API）
- [ ] 21. 手动验证：启动 docker-compose，上传文档，确认 document_process -> rag_index 链路正常 -- **待用户操作**

### 4.4 Spec Review Notes (待 review_spec 填充)

待 Plan Approved 后，用户可执行 `review_spec` 进行建议性预审。

---

## 5. Execute Log

### 5.1 首次执行（commit 55b1d78，2026-08-05）

- **执行范围**：30 文件，+1679/-1420
- **新增**：`middleware.py` / `span_tracker.py` / `reaper.py` / `constants.py` / `002_dramatiq_migration.py` / `task-queue-design.md`
- **重写**：`broker.py` / `tasks.py` / `runtime.py` / `executor.py`
- **删除**：`outbox/` 整个目录（6 文件）+ 3 个旧测试文件
- **修改**：`settings.py` / `pyproject.toml` / `docker-compose.yml` / `rag_ingestion.py` / `document.py` / `task_runtime.py` / `errors.py` / `schemas.py` / `__init__.py`
- **验证**：`ruff check` All checks passed
- **未完成**：checklist #20（28 个测试适配，待后续任务）/ #21（手动 docker-compose 验证，待用户操作）

### 5.2 review_execute 后修复（2026-08-05）

review_execute 评审发现 1 个 Blocking Issue（重试语义矛盾，经核 `task-queue-design.md` 确认为已知限制，归属 Q1）+ 4 个质量改进项，本次修复如下：

| # | 文件 | 修复内容 |
|---|---|---|
| F2 | `002_dramatiq_migration.py` | upgrade 前加 `DELETE` 清理 processing_spans 历史重复行（保留每组 run_id+span_name 最新一行），避免唯一索引创建失败；修正 `ix_processing_runs_status_heartbeat` 索引名 |
| F3 | `rag_ingestion.py` | 4 处 `fail_span` 的 `error_code` 从 `type(exc).__name__` 改为 `getattr(exc, "error_code", ErrorCode.UNEXPECTED_EXCEPTION)`，统一稳定机器错误码 |
| F4 | `rag_ingestion.py` + `document.py` | 两处 `enqueue_run` 调用外加 try/except，入队失败时调 `mark_run_enqueue_failed` 立即标 Run failed（启用原死代码，不等 Reaper 5min 兜底） |
| F5 | `runtime.py` | `start_worker_attempt` 加注释说明并发安全假设（依赖 `claim_run` CAS 保证不并发） |

- **验证**：`ruff check` All checks passed

---

## 6. Review Verdict

### 6.1 Review Matrix

| 轴 | 项 | 评定 | 证据 |
|---|---|---|---|
| 轴1 Spec质量 | 目标/范围/决策 | PASS | §1 Goal/In-Scope/Out-Scope 清晰，§2.5 grilling 21 题决策表完整 |
| 轴1 | 验收闭环 | PARTIAL | checklist 19/21 完成，#20 测试适配 + #21 手动验证未完成（已标注待办） |
| 轴1 | Spec 同步 | PASS（修复后）| 首次执行时未同步（FAIL），本次 review_execute 已回写头部 Phase + §5-§8 |
| 轴2 一致性 | 文件/签名/删除 | PASS | §4.1 文件清单与代码一致，§4.2 签名完全匹配 |
| 轴2 | constants.py 未登记 | PASS（修复后）| 首次未登记（偏差），本次 §4.1 已补登 |
| 轴2 | enqueue_run 字面量不一致 | PARTIAL | `document.py` 用字符串 `"document_process"`，`rag_ingestion.py` 用 `RunType.RAG_INDEX`；技术上不违规，风格不统一，记后续统一 |
| 轴3 质量 | 重试语义矛盾 | PARTIAL | `_handle_failure` 对 Transient 错误调 `fail_run` 导致 Dramatiq 重试失效；经核 `task-queue-design.md` §4.3/§7 确认为已知限制，归属 Q1 整体处理，本次不修 |
| 轴3 | fail_span error_code 混乱 | PASS（修复后）| F3 已修 |
| 轴3 | mark_run_enqueue_failed 死代码 | PASS（修复后）| F4 已启用 |
| 轴3 | 迁移历史数据冲突 | PASS（修复后）| F2 已加 DELETE 预处理 |
| 轴3 | attempt 计数非原子 | PASS（修复后）| F5 已加注释说明 CAS 假设 |
| 轴3 | broker 模块级副作用 | PARTIAL | import 时建 Redis 连接，风险记后续评估 |
| 轴3 | Reaper 多副本无去重 | PARTIAL | 当前单容器不阻塞，记已知限制 |
| 轴3 | handler 重复加载 | PARTIAL | `importlib` 有缓存，性能小瑕疵，记后续优化 |

### 6.2 Overall Verdict

**PASS**（条件性）

- Blocking Issue（重试语义）经核为已知限制，归属 Q1，本次不修
- 4 项质量改进（F2-F5）已修复并通过 ruff
- 二轮修复（T2/T6/T7/T9/T-asyncio）已完成：测试重写 + asyncio.run 修复 + handler 加载优化 + 风格统一
- 验收闭环尚有 #21 手动 docker-compose 验证待完成（#20 测试适配已完成，待 DB 环境运行验证）
- Spec 已同步

### 6.3 Blocking Issues

无（重试语义矛盾已确认为已知限制，非本次 Blocking）。

### 6.4 后续任务清单

| # | 任务 | 状态 | 来源 |
|---|---|---|---|
| T1 | Q1 handler 幂等策略重写 + 自动重试重置 Run 为 pending（含 attempt_no 递增问题）| 待办 | §0 Q1 |
| T2 | 3 个测试文件适配新 API（30 个测试用例已重写，collection 通过，待 DB 环境运行验证）| **已完成代码**，待运行验证 | checklist #20 |
| T3 | 手动 docker-compose 验证 document_process -> rag_index 链路 | 待办 | checklist #21 |
| T4 | broker 模块级初始化副作用评估（惰性初始化可行性）| 待办 | review §3.3 |
| T5 | Reaper 多副本去重（`SELECT FOR UPDATE SKIP LOCKED`）或 spec 声明单副本约束 | 待办 | review §3.4 |
| T6 | `enqueue_run` 调用风格统一（`RunType.X` vs 字符串字面量）| **已完成** | review §3.2 |
| T7 | handler `_load_handlers` 重复加载优化 | **已完成**（改为首次执行惰性加载 + `_handlers_loaded` 标志）| review §3.7 |
| T-asyncio | `RunFailureMiddleware` 的 `asyncio.run()` 跨 loop 风险 | **已完成**（改用 `get_event_loop_thread().run_coroutine()` + fallback）| review 子代理验证 |
| T9 | f-string 日志改 `%s` | **已完成** | review §3.9 |

---

## 7. Plan-Execution Diff

| 偏差项 | Plan 描述 | 实际执行 | 原因 | 处理 |
|---|---|---|---|---|
| `constants.py` 未登记 | §4.1 新增文件表无 `constants.py` | 代码中存在且被多处引用 | 实现时合理抽取常量集中管理 | §4.1 已补登 |
| 迁移脚本未处理历史重复 span | §4.1 只写"删表 + 删字段 + 加 attempt_no" | F2 补加 DELETE 预处理 | 旧表无 attempt_no，重试产生的同 (run_id, span_name) 多行会违反新唯一索引 | F2 已修 |
| `mark_run_enqueue_failed` 未调用 | §4.2 签名定义了但无调用方 | F4 启用，两处 enqueue_run 外加 try/except | 原 Plan 只说"入队失败靠 Reaper 兜底"，实现时发现可立即标 failed 更优 | F4 已修 |
| `fail_span` error_code 用类名 | §4.2 签名未约束 error_code 来源 | F3 改用 `getattr(exc, "error_code", ...)` | 原实现传 `type(exc).__name__` 混入 Python 类名，破坏稳定机器错误码意图 | F3 已修 |
| 重试语义矛盾 | §2.5 决策 #6 "重试交 Dramatiq" | 实际重试失效（`_handle_failure` 标 failed 后重试被吸收）| Plan §4.1 只写"_handle_failure 简化为只调 fail_run"，未区分 Terminal/Transient | 确认为已知限制，归属 Q1，§0 Q1 + §2.5 #6 已补注 |

---

## 8. 已知限制（review_execute 补充）

| 限制 | 影响 | 缓解/归属 |
|---|---|---|
| **Dramatiq 自动重试不重跑 handler** | Transient 错误（如 embedding API 限流）不会自动重试，Run 直接进 failed，需用户手动重试 | 已知限制，Q1 整体处理（reset Run + handler 幂等改造 + attempt_no 递增）。详见 `mydocs/task-queue-design.md` §4.3/§7 |
| **Reaper 单副本约束** | 多 worker 副本部署时，多个 Reaper 线程并发扫到同批 stalled_ids，产生无谓写冲突 | **TODO（T5）**：当前单容器部署不阻塞；多副本前需加 `SELECT FOR UPDATE SKIP LOCKED` 或 spec 显式声明禁止多副本 |
| **broker 模块级初始化** | `import app.workers` 即建 Redis 连接，Redis 不可用时 import 崩 | **TODO（T4）**：当前开发/部署环境 Redis 稳定；后续评估惰性初始化 |
| **双写窗口** | DB 提交后入队失败，Run 立即被 `mark_run_enqueue_failed` 标 failed（F4 已修）| 已缓解，不再等 Reaper 5min |
| **commit_success 失败的边缘情况** | DB 临时故障导致 `complete_run` 失败，业务可能已部分写入 | 接受不完美（§2.4 风险 1）|
| **Dramatiq time_limit 与 span 心跳的交互** | worker 崩溃后 60s 重投，新 worker `claim_run` 失败，Run 卡 running 5-10min | 恢复延迟可接受 |
| **`max_retries` 等参数硬编码** | `tasks.py` 中 `max_retries=3` 等未从 settings 读取 | 后续可改为从 settings 读取 |
| ~~`asyncio.run()` 跨 loop 风险~~ | ~~`RunFailureMiddleware` 用 `asyncio.run()` 建新 loop，与 EventLoopThread 的 asyncpg 连接池冲突~~ | **已修复（T-asyncio）**：改用 `get_event_loop_thread().run_coroutine()` 复用 EventLoopThread 的 loop，非 worker 上下文 fallback 到 `asyncio.run()` |
| ~~handler 重复加载~~ | ~~`__init__.py` + `tasks.py` 重复调 `_load_handlers()`~~ | **已修复（T7）**：去掉 `__init__.py` 模块级调用，改为 actor 首次执行时惰性加载 + `_handlers_loaded` 标志防重复 |

---

## 9. Archive Record

待任务闭环后填充。
