# RAG / Wiki 数据库表逐表审批记录

> 状态：进行中<br>
> 最后更新：2026-07-27<br>
> 规则：逐表说明用途、字段和生命周期；用户批准前不创建表、不修改 migration。

## 已确认的全局决策

1. Wiki 与 RAG 在 MVP 中独立运行：Wiki 页面不切块、不 embedding、不写入 RAG 召回池。
2. 数据库不建立显式外键约束。表之间保留逻辑引用字段和查询索引；应用服务负责关联校验、删除顺序与孤儿数据巡检。
3. `knowledge_bases` 是轻量共享根，不保存 RAG/Wiki 类型、嵌入模型或能力开关。
4. RAG 与 Wiki 分别使用独立配置表。
5. 所有候选表完成审批后，再按依赖顺序统一实现 migration；当前没有创建任何数据库表。

## 审批进度

共 15 张候选表，已批准 4 张，待审批 11 张。

| # | 分组 | 表 | 状态 | 核心职责 |
|---|---|---|---|---|
| 1 | 共享根 | `knowledge_bases` | 已批准 | RAG/Wiki 共享的数据归属与生命周期边界 |
| 2 | 共享配置 | `kb_rag_configs` | 已批准 | RAG 启停、召回方式、嵌入模型与分块配置 |
| 3 | 共享配置 | `kb_wiki_configs` | 已批准 | Wiki 启停、生成模式、生成模型与生成策略 |
| 4 | RAG | `sources` | 已批准 | 文档进入 KB 的来源或连接器 |
| 5 | RAG | `documents` | 待审批 | 逻辑文档身份与当前状态 |
| 6 | RAG | `document_revisions` | 待审批 | 文档每次上传或同步形成的不可变版本 |
| 7 | RAG | `content_chunks` | 待审批 | 文档版本分块后的检索单元 |
| 8 | Wiki | `wiki_pages` | 待审批 | Wiki 页面正文、类型、slug 与编辑版本 |
| 9 | Wiki | `wiki_page_links` | 待审批 | 页面间由 wikilink 形成的有向链接 |
| 10 | Wiki | `wiki_page_document_refs` | 待审批 | Wiki 页面到原始文档的文档级血缘 |
| 11 | Wiki | `wiki_page_chunk_refs` | 待审批 | Wiki 页面到原始 chunk 的证据级血缘 |
| 12 | Wiki | `wiki_folders` | 待审批 | Wiki 目录树和导航路径 |
| 13 | 运行支撑 | `processing_runs` | 待审批 | 解析、嵌入和 Wiki 生成任务的运行记录 |
| 14 | 运行支撑 | `processing_spans` | 待审批 | 一次处理运行内部各阶段的耗时与错误 |
| 15 | 运行支撑 | `search_logs` | 待审批 | 检索查询、命中和评测反馈 |

## 已批准表摘要

### `knowledge_bases`

保留内部 BIGINT ID 与对外 UUID；保存归属、名称、描述和软删除时间。不保存 `type`、embedding 字段或 `*_enabled` 开关。

### `kb_rag_configs`

保存 `enabled`、`vector_enabled`、`keyword_enabled`、`embedding_model_key`、`embedding_dim`、`chunking_strategy`、`chunk_size_tokens`、`chunk_overlap_tokens`、`chunking_options` 和 `config_version`。不保存 Wiki 配置、top-k、RRF 参数或 boost。

### `kb_wiki_configs`

保存 `enabled`、`generation_mode`、`generation_model_key`、`generation_options` 和 `config_version`。MVP 的生成模式为 `manual` 或 `on_ingest`；不包含 embedding 或 Wiki chunk boost。

### `sources`

保存来源身份、`source_type`、显示名称、启停状态、适配器配置、增量同步游标、配置版本和最近成功同步时间。敏感凭据只保存引用，不写入 `config` 明文。

## 下一步

下一轮从表 5 `documents` 开始。重点先确认“逻辑文档”与“每次上传/同步版本”的边界，再决定是否保留独立的 `document_revisions`。
